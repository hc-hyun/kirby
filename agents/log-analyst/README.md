# log-analyst

Separate observations and hypotheses in Android logs.

Edit agent.yaml, skills and schemas together. Fixtures are synthetic examples, not
production data. Shared file-evidence guidance is resolved from _shared.
